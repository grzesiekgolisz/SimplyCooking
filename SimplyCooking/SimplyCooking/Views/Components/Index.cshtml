@model IEnumerable<SimplyCooking.Models.Component>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Recipe.UserID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Recipe.UserID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComponentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComponentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComponentID })
        </td>
    </tr>
}

</table>
